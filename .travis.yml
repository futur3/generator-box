language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-box' ]; then cd .. && eval "mv $currentfolder generator-box" && cd generator-box; fi

